# Frontend Next (Experiment)

This folder is a Next.js App Router port of the existing Vite frontend for migration experiments.

## Run

```bash
npm install
npm run dev
```

Use `NEXT_PUBLIC_API_BASE` in `.env.local` if backend URL differs from `http://localhost:8000/api`.
